import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Incidents: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Incidents Page Title</Title>
  </PageSection>
)

export { Incidents };
