import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const DisasterLocation: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">Disaster Location Page Title</Title>
  </PageSection>
)

export { DisasterLocation };
